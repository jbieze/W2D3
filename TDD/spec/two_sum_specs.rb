require 'rspec'
require 'two_sum'

describe "#two_sum" do
  subject(:array) { [-1, 0, 2, -2, 1] }

  it "returns an array of arrays" do
    context "if there are a pair that sum to 0" do
      #
    end
  end

  it ""
end
